<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> Posts </h1>
    <% if @post.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@post.errors.count, "error") %> prohibited this user from being saved:</h2>

            <ul>
            <% @post.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
            </ul>
        </div>
    <% end %>

    <%= form_with model: @post do |form| %>
        <div>
            <%= form.label :title, "Enter your title" %>
            <%= form.text_field :title, placeholder: "awesome day"%>
        </div>
        <div>
            <%= form.label :body %>
            <%= form.text_field :body, placeholder: "It was a sunny bright..." %>
        </div>
        <%= form.submit %>
    <% end %>
</body>
</html>